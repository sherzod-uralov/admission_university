import React from "react";

export interface storeProviderChildren {
  children: React.ReactNode;
}

export interface navBarMenuType {
  isOpen: boolean;
}

export interface changeLanguageInterface {
  path: string;
}
