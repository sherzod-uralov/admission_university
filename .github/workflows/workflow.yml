name: Simple Test

on:
  push:
    branches: [ "main", "master" ]

jobs:
  test:
    runs-on: self-hosted
    
    steps:
    - name: Test basic functionality
      run: |
        Write-Host "✅ GitHub Actions ishlayapti!"
        Write-Host "Repository: ${{ github.repository }}"
        Write-Host "Vaqt: $(Get-Date)"
        Write-Host "Runner ID: $env:COMPUTERNAME"
      shell: powershell
